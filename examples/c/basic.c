// Basic arithmetic and functions in C
int sum(int a, int b, int c) {
    int result = a + b + c;
    return result;
}

int main() {
    int a = 10;
    int b = 20;
    int c = 30;
    int total = sum(a, b, c);
    return total; // 60 in a0
}
