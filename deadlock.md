Create a single-file HTML/CSS/JS interactive deadlock teaching tool. Dark theme (#0a0e27). 2 processes (circles, r=48) + 2 resources (rounded rects, 104×68) in a diamond layout inside SVG viewBox="0 0 460 380" with positions: P1(110,115) P2(350,265) R1(350,115) R2(110,265). Three modes via top tab bar: (1) 💀 Deadlock — 6 steps showing both processes acquiring resources in opposite order leading to circular wait; (2) 🛡️ Prevention — 7 steps with ordered resource acquisition (R1 before R2) breaking circular wait; (3) 🔮 Avoidance — 7 steps using Banker's Algorithm where an unsafe request is denied. Each step stores: description HTML, holds/wants per process, arrow list (from/to/type:hold|wait|denied, animated flag), statuses, active Coffman conditions, deadlock/success flags, banker state, log text. Arrows use quadratic Bézier curves with border-intersection start/end points, deterministic curve-side per entity-pair so parallel arrows don't overlap, and dashed animation for wait/denied types. Arrow labels positioned at t=0.45 on the Bézier, offset perpendicular to the tangent. Side panel: step description card, resource allocation table, Coffman conditions grid (highlights active in red, broken in green with strikethrough), scrollable event log, legend. Controls: prev/next/reset/skip-to-end buttons + keyboard (←→ Space R Home End 1-2-3) + click-to-advance on SVG + touch swipe. Overlay banners for DEADLOCK (red) and COMPLETED (green) with pulse animations. Responsive: flex row on desktop, column on ≤960px, compact on ≤480px. Entity text: 14px process names, 20px emoji icons, 13px resource names, 11px subtitles. Green/red availability dots on resources.

